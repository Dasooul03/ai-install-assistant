package com.example.installassistant.config;

import org.springframework.context.annotation.Configuration;

/**
 * Milvus 向量存储配置
 */
@Configuration
public class MilvusConfig {
    // TODO: Phase 3 — 手动配置 MilvusServiceClient (可选, auto-config 已覆盖)
}
